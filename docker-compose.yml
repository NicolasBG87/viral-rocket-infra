services:
  processor:
    image: video-processor-dev
    build:
      context: .
      dockerfile: Dockerfile
    command: python3 run.py
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [ gpu ]
    volumes:
      - .:/app
      - ./input:/app/input
      - ./output:/app/output
