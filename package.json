{
  "name": "viral-rocket-infra",
  "version": "1.0.0",
  "scripts": {
    "docker:build:clean": "docker compose build --no-cache",
    "docker:run": "docker compose up",
    "docker:build": "docker build -t nbojanovic/video-processor:latest .",
    "docker:push": "docker push nbojanovic/video-processor:latest"
  }
}
